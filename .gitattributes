# Normalize all text files to LF in the repository AND on checkout.
# Windows editors may still show CRLF while typing; git stores and checks out LF.
* text=auto eol=lf

# Binary files: never touch line endings.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.woff binary
*.woff2 binary
*.ttf binary
